services:
  claim-controller:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    image: ghcr.io/ia-generative/claim-controller:dev
    mem_limit: 2g
    ports:
      - "8080:8080"
      - "8081:8081"
      - "8082:8082"
    volumes:
      - ./:/workspace
      - ./config/template/values.yaml:/values/values.yaml:ro
      - ${HOME}/.kube:/root/.kube:ro
    environment:
      VALUES_PATH: /values/values.yaml
      KUBECONFIG: /root/.kube/config