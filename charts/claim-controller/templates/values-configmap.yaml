apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "claim-controller.fullname" . }}-values
data:
  values.yaml: |
{{ .Values.valuesTemplate | indent 4 }}
{{ .Values.extraValuesTemplate | indent 4 }}
